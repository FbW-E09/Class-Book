let isRaining = false;
const action = isRaining ? "stay at home" : "go to the park";
// console.log(action);

const sentence = `Today I will ${action}`;
// const sentence = `Today I will ${isRaining ? "stay at home" : "go to the park"}`;
console.log(sentence);