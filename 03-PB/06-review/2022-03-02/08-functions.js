// a and b are parameters
function sum(a, b) {
  return a + b;
}

let multiply = function (a, b) {
  return a * b;
};

let sum2 = (a, b) => {
  return a + b;
};

let sum3 = (a, b) => a + b;

// 1 and 2 are arguments. They will be assigned to the parameters "a" and "b" inside the function
sum(1, 2); 